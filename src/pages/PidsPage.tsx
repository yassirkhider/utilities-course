import ResourceLibraryPage from './ResourceLibraryPage';

export default function PidsPage() {
  return <ResourceLibraryPage resourceType="pid" title="P&ID Library" subtitle="Training piping & instrumentation diagrams. Verify against approved plant documentation." />;
}
