import ResourceLibraryPage from './ResourceLibraryPage';

export default function HandoutsPage() {
  return <ResourceLibraryPage resourceType="handout" title="Handout Library" subtitle="Printable posters and reference handouts for each topic." />;
}
